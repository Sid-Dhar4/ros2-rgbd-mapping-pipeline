# 60-Second Demo Video Script

1. Show the repo title and goal:
   "This is a ROS 2 RGB-D mapping pipeline using TUM RGB-D sequences."

2. Run:
   ```bash
   ros2 launch rgbd_mapping_pipeline rviz_mapping.launch.py
